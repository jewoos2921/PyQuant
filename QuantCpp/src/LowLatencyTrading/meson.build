subdir('common')
subdir('exchange')

# 예시: common과 exchange의 소스 파일을 모아서 라이브러리 생성
# common_sources = files('common/file1.cpp', 'common/file2.cpp')
# exchange_sources = files('exchange/file1.cpp')
# liblowlatencytrading = static_library('lowlatencytrading', common_sources + exchange_sources)
