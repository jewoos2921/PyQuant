{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Meson Build",
            "type": "shell",
            "command": "meson compile -C builddir",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}